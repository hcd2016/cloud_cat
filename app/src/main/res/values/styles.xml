<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- 解决启动页刚启动时白屏问题，实现秒启动-->
    <style name="AppTheme.Launcher" parent="AppTheme">
        <item name="android:windowBackground">@drawable/shape_splash_bg</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!--主页tab-->
    <style name="main_rb_item" parent="AppTheme">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">0px</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">10sp</item>
        <item name="android:button">@null</item>
        <item name="android:clickable">true</item>
        <item name="android:paddingTop">9dp</item>
        <item name="android:paddingBottom">3dp</item>
        <item name="android:textColor">@drawable/selector_menu_radio</item>
    </style>

    <!--透明背景dialog样式-->
    <style name="transparent_dialog_style" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!--不带动画的对话框style-->
    <style name="style_dialog_without_anim" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!--    白se按钮点击效果style-->
    <style name="white_btn_click_style" parent="AppTheme">
        <item name="android:background">@drawable/selector_rect_white_gray_click</item>
        <item name="android:clickable">true</item>
    </style>

    <!--    白se按钮点击效果style圆角8-->
    <style name="white_btn_click_style_c8" parent="AppTheme">
        <item name="android:background">@drawable/selector_rect_white_gray_click_c8</item>
        <item name="android:clickable">true</item>
    </style>
</resources>
